/**
 * Wordbun theme configuration
 */

import type { ThemeConfig } from '~types/config';

const themeConfig: Partial<ThemeConfig> = {
  colors: {
    primary: '#6b2d6b',
    primaryLight: '#a678b8',
    primaryDark: '#4b184b',
  },
};

export default themeConfig;
